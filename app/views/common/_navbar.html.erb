<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <% if role_signed_in? then %>
    <a class="navbar-brand" href="/works/<%= current_role.workurl %>"><%= Work.find(current_role.work_id).w_name %></a>
  <% else %>
    <a class="navbar-brand" href="/top">タイトル</a>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/">トップ <span class="sr-only">(current)</span></a>
      </li>
      <% if @work != nil %>
        <li class="nav-item">
            <a class="nav-link" href="/works/<%= @work.w_url %>"><%= @work.w_name %></a>
        </li>
      <% end %>
      <li class="nav-item">
        <% if role_signed_in? %>
          <%= link_to "ログアウト", destroy_role_session_path, method: :delete %>
        <% end %>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
      </li>
    </ul>
  </div>
</nav>